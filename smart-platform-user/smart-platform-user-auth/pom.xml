<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <artifactId>smart-platform-user-auth</artifactId>

    <parent>
        <groupId>net.guerlab.smart-platform</groupId>
        <artifactId>smart-platform-user</artifactId>
        <version>${revision}</version>
    </parent>

    <properties>
        <main.basedir>${basedir}/../..</main.basedir>
    </properties>

    <dependencies>
        <dependency>
            <groupId>net.guerlab.smart-platform</groupId>
            <artifactId>smart-platform-basic-auth</artifactId>
        </dependency>

        <dependency>
            <groupId>net.guerlab.smart-platform</groupId>
            <artifactId>smart-platform-user-core</artifactId>
        </dependency>

        <dependency>
            <groupId>net.guerlab.smart-platform</groupId>
            <artifactId>smart-platform-user-service</artifactId>
            <scope>provided</scope>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-configuration-processor</artifactId>
            <optional>true</optional>
        </dependency>
    </dependencies>

</project>
