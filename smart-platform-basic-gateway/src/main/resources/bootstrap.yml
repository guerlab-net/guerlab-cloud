server:
  port: 8080
nacos:
  hostname: nacos
  port: 8848
  namespace:
spring:
  main:
    banner-mode: 'OFF'
  application:
    name: gateway
  cloud:
    nacos:
      config:
        server-addr: ${nacos.hostname}:${nacos.port}
        file-extension: yml
        namespace: ${nacos.namespace}
        shared-configs:
          - data-id: global.yml
          - data-id: gateway.yml
      discovery:
        server-addr: ${nacos.hostname}:${nacos.port}
        namespace: ${nacos.namespace}
logging:
  level:
    root: ERROR
    net.guerlab.smart.platform: INFO
