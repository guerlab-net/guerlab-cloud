<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <groupId>net.guerlab.smart-platform</groupId>
    <artifactId>smart-platform-parent</artifactId>
    <version>${revision}</version>
    <packaging>pom</packaging>

    <properties>
        <revision>0.1.0</revision>
        <main.basedir>${basedir}</main.basedir>

        <java.version>11</java.version>
        <maven-gpg-plugin.version>1.6</maven-gpg-plugin.version>
        <plugin.javadoc.version>2.10.2</plugin.javadoc.version>
        <plugin.deploy.version>2.8.2</plugin.deploy.version>
        <project.encoding>UTF-8</project.encoding>

        <guerlab-spring.version>2.3.5</guerlab-spring.version>

        <spring-cloud-alibaba.version>2.1.1.RELEASE</spring-cloud-alibaba.version>

        <jjwt.version>0.9.1</jjwt.version>
        <guava.version>20.0</guava.version>
        <pagehelper.version>1.2.12</pagehelper.version>
    </properties>

    <distributionManagement>
        <repository>
            <id>rdc-releases</id>
            <url>https://repo.rdc.aliyun.com/repository/2451-release-8qdhKz/</url>
        </repository>
        <snapshotRepository>
            <id>rdc-snapshots</id>
            <url>https://repo.rdc.aliyun.com/repository/2451-snapshot-kGepUG/</url>
        </snapshotRepository>
    </distributionManagement>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <configuration>
                        <source>${java.version}</source>
                        <target>${java.version}</target>
                        <encoding>${project.encoding}</encoding>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-resources-plugin</artifactId>
                    <configuration>
                        <encoding>${project.encoding}</encoding>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-javadoc-plugin</artifactId>
                    <version>${plugin.javadoc.version}</version>
                    <configuration>
                        <encoding>${project.encoding}</encoding>
                        <aggregate>true</aggregate>
                        <charset>${project.encoding}</charset>
                        <docencoding>${project.encoding}</docencoding>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>
        <plugins>
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>flatten-maven-plugin</artifactId>
                <version>1.1.0</version>
                <configuration>
                    <updatePomFile>true</updatePomFile>
                    <flattenMode>resolveCiFriendliesOnly</flattenMode>
                </configuration>
                <executions>
                    <execution>
                        <id>flatten</id>
                        <phase>process-resources</phase>
                        <goals>
                            <goal>flatten</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>flatten.clean</id>
                        <phase>clean</phase>
                        <goals>
                            <goal>clean</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>net.guerlab.spring</groupId>
                <artifactId>guerlab-spring</artifactId>
                <version>${guerlab-spring.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <dependency>
                <groupId>com.alibaba.cloud</groupId>
                <artifactId>spring-cloud-alibaba-dependencies</artifactId>
                <version>${spring-cloud-alibaba.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!-- smart-platform basic -->
            <dependency>
                <groupId>net.guerlab.smart-platform</groupId>
                <artifactId>smart-platform-basic-api</artifactId>
                <version>${revision}</version>
            </dependency>
            <dependency>
                <groupId>net.guerlab.smart-platform</groupId>
                <artifactId>smart-platform-basic-auth</artifactId>
                <version>${revision}</version>
            </dependency>
            <dependency>
                <groupId>net.guerlab.smart-platform</groupId>
                <artifactId>smart-platform-basic-gateway</artifactId>
                <version>${revision}</version>
            </dependency>
            <dependency>
                <groupId>net.guerlab.smart-platform</groupId>
                <artifactId>smart-platform-basic-swagger</artifactId>
                <version>${revision}</version>
            </dependency>

            <!-- smart-platform commons -->
            <dependency>
                <groupId>net.guerlab.smart-platform</groupId>
                <artifactId>smart-platform-commons</artifactId>
                <version>${revision}</version>
            </dependency>

            <!-- smart-platform server parent -->
            <dependency>
                <groupId>net.guerlab.smart-platform</groupId>
                <artifactId>smart-platform-server-parent</artifactId>
                <version>${revision}</version>
            </dependency>

            <!-- smart-platform stream -->
            <dependency>
                <groupId>net.guerlab.smart-platform</groupId>
                <artifactId>smart-platform-stream</artifactId>
                <version>${revision}</version>
            </dependency>

            <!-- smart-platform user -->
            <dependency>
                <groupId>net.guerlab.smart-platform</groupId>
                <artifactId>smart-platform-user-api</artifactId>
                <version>${revision}</version>
            </dependency>
            <dependency>
                <groupId>net.guerlab.smart-platform</groupId>
                <artifactId>smart-platform-user-auth</artifactId>
                <version>${revision}</version>
            </dependency>
            <dependency>
                <groupId>net.guerlab.smart-platform</groupId>
                <artifactId>smart-platform-user-core</artifactId>
                <version>${revision}</version>
            </dependency>
            <dependency>
                <groupId>net.guerlab.smart-platform</groupId>
                <artifactId>smart-platform-user-service</artifactId>
                <version>${revision}</version>
            </dependency>
            <dependency>
                <groupId>net.guerlab.smart-platform</groupId>
                <artifactId>smart-platform-user-stream</artifactId>
                <version>${revision}</version>
            </dependency>
            <dependency>
                <groupId>net.guerlab.smart-platform</groupId>
                <artifactId>smart-platform-user-web</artifactId>
                <version>${revision}</version>
            </dependency>

            <dependency>
                <groupId>io.jsonwebtoken</groupId>
                <artifactId>jjwt</artifactId>
                <version>${jjwt.version}</version>
            </dependency>

            <dependency>
                <groupId>com.google.guava</groupId>
                <artifactId>guava</artifactId>
                <version>${guava.version}</version>
            </dependency>

            <dependency>
                <groupId>com.github.pagehelper</groupId>
                <artifactId>pagehelper-spring-boot-starter</artifactId>
                <version>${pagehelper.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
        </dependency>
    </dependencies>

    <modules>
        <module>smart-platform-basic</module>
        <module>smart-platform-commons</module>
        <module>smart-platform-server-parent</module>
        <module>smart-platform-user</module>
        <module>smart-platform-stream</module>
    </modules>
</project>
